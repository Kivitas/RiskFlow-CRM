# RiskFlow CRM does not require environment variables for local-first use.
# AI provider keys are configured in Settings inside the app.
# Email and WhatsApp currently use browser workflow links. API integrations can be added later.
